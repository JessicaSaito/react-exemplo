const MainMenu = () => {

    const navStyle = {
        background: '#555',
        color: '#000',
        padding: '20px',
        marginTop: '5px'
    }

    return (
      <nav>Menu...</nav>
    )

    return (
        <nav navStyle={
            {
                background: '#555',
                color: '#000',
                padding: '20px',
                marginTop: '5px'
            }
        }>Menu...</nav>
      )
  }